<Page
    x:Class="Yugen.DJ.Uwp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Yugen.DJ.Uwp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <StackPanel Spacing="4">

            <Button Content="Open"
                    Command="{x:Bind ViewModel.OpenCommand}" />

            <ToggleButton Content="Play"
                          IsChecked="{x:Bind ViewModel.IsPlaying, Mode=TwoWay}" />
        </StackPanel>

        <StackPanel Grid.Column="1" 
                    Spacing="4">

            <Button Content="Open"
                    Command="{x:Bind ViewModel.OpenCommand2}" />

            <ToggleButton Content="Play"
                          IsChecked="{x:Bind ViewModel.IsPlaying2, Mode=TwoWay}" />

        </StackPanel>

    </Grid>
</Page>
